
def get_num_unique_users(data)
	
	dic = {}
	for i in data:

		complete =  name.split(",")
		name = complete[1]

		if name in dic.keys:
			dic[name] = 1
		else:
			dic[name] = 1

	total = len(dic)



data =  get_csv(pwd/nameFile.csv)

###############################################
















